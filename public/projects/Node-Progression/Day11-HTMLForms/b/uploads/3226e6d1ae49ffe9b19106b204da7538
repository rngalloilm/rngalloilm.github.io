To run docker in root folder:
docker-compose up

Initialize a new Node.js project:
npm install
npm init -y

To edit while running:
npm install --save-dev nodemon

For input requests:
npm install multer

Route Handling:
app.get('/', (req, res) => {
    res.sendFile(path.join(templatesPath, 'index.html'))
});

app.get('/company/about', (req, res) => {
    res.sendFile(path.join(templatesPath, 'about.html'))
});
// If none of the others execute, this will
app.all('*', (req, res) => {
    res.sendFile(path.join(templatesPath, '404.html'))
});

.ssh:
C:\Users\jenga\.ssh

ssh Individual VM in powershell:
ssh rngallo@csc342-135-host.csc.ncsu.edu

To view files:
nano __

Working Directory:
cd /var/webfolder

Dubious ownership means it is inconsistent, can use a command to bypass.

EX, args in a GET request:
https://httpbin.org/get?name=John&age=30
https://httpbin.org/post
https://httpbin.org/put
https://httpbin.org/delete